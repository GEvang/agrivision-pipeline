# Deployment view

AgriVision Pipeline exposes one public operational surface at the repository root: installer, Dockerfile, compose file, and entrypoint. The `deployment/examples/` directory is internal reference material rather than a second deployment path.
