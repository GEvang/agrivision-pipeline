# Architecture overview

AgriVision Pipeline keeps a transport-agnostic processing core and two supported adapters: the CLI and the dashboard. Runtime and container support are defined by the root operational assets rather than duplicated wrapper layers.
