# Troubleshooting

- Check `python run.py --doctor`
- Verify `.env` secrets are present
- Confirm Docker is available for ODM execution
- Review `output/weather` and `output/irrigation` artifacts for integration failures
